{% extends 'base.html.twig' %}

{% block title %}AnalysisType{% endblock %}

{% block body %}
    <h1>AnalysisType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ analysis_type.id }}</td>
            </tr>
            <tr>
                <th>Analysis_name</th>
                <td>{{ analysis_type.analysisName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('analysis_type_index') }}">back to list</a>

    <a href="{{ path('analysis_type_edit', {'id': analysis_type.id}) }}">edit</a>

    {{ include('analysis_type/_delete_form.html.twig') }}
{% endblock %}
